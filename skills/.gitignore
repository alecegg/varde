# macOS
.DS_Store

# Skill eval-run outputs — regenerated by scripts/run-output-evals.sh and
# stripped from installs by install.sh; never commit them to source.
*-workspace/
benchmark.json

# Local working plans, reviews, friction, and session artifacts.
memory-bank/working/
memory-bank/friction/
