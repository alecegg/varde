# Rust build output (per-folder .gitignores also cover this)
**/target/

# Durable project knowledge is committed.
# Working state (including working/friction/) remains local.
**/memory-bank/working/
# Legacy friction location, kept ignored so stale local copies never get committed.
**/memory-bank/friction/

# macOS
.DS_Store
