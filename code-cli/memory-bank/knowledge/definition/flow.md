---
slug: flow
type: definition
definition: >-
  The full reachable-tree walk from one semantic-entrypoint through the
  resolved call graph, rendered as a named path (e.g. "POST /users ->
  UserController.create -> UserService.save").
avoid:
  - application flow
---
